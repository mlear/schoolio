<%= form_for @grade do |f| %>
	<%= f.label :gpa %>
	<%= f.text_field :gpa %>

	<%= f.label :course %>
	<%= f.collection_select :course, Course.all, :id, :name %>

	<%= f.submit "Add grade" %>
<% end %>