<%= form_for @grade do |f| %>
	<%= f.label :gpa %>
	<%= f.text_field :gpa %>

	<%= f.label :course_id %>
	<%= f.collection_select :course_id, Course.all, :id, :name %>

	<%= f.submit "Add grade" %>
<% end %>