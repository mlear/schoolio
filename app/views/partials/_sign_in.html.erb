<ul>
  <%= form_for :session, url: sessions_path do |f| %>

    <%= f.label :email, 'email' %>
    <%= f.text_field :email %>

    <%= f.label :password, 'password' %>
    <%= f.password_field :password %>

    <%= f.submit :sign_in %>

  <%end%>
</ul>
